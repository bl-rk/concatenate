# concat()



A minimalist React Native (Expo) dating app prototype. A match is `"You" + "another"` == `"CONCATENATED"` .

## Stack
React Native (Expo), TypeScript, React Navigation, Reanimated + Gesture Handler.

## Run
```bash
npm install
npx expo start
```

## Status
 In progress — foundation and screens scaffolded.